Upgrade dependencies
Switch to rollup
Figure out what to call two exports
Split into two separate classes (still all one file, tree shaking can reduce it)
Fix tests
Add tests for error when importing wrong class in host/worker
Confirm tree shaking works in BBGM
